<%= simple_form_for @trait do |f|%>
  <%= f.input :name %>
  <!-- go to schema and change description to a text input instead of string -->
  <%= f.input :description %>
  <%= f.input :price_hour %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
